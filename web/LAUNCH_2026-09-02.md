# Mara Vera — First Public Launch

Date: 2026-09-02

## Founder decision

Founder explicitly chose to skip the pre-launch tester round and launch a minimal public version now.

This does **not** authorize merging PR #3 or PR #4. The standing merge rule remains: merge only after the founder says exactly `mergea`.

## Launch posture

The first public release is deliberately:

- adults-only;
- AI-disclosed;
- text-first;
- free / no real payments;
- no public P0 labs;
- no external adult-media integration;
- no real Caprichos contributions;
- no payment provider;
- no fake canonical voice;
- no fake scarcity.

Public product surface:

`Home → /experience → local continuity / return`

The public `/experience` route now points to `LaunchExperience`, while P0/lab infrastructure remains separate for internal development.

## Vercel production deployment

Created through the connected Vercel deployment tool.

- Deployment ID: `dpl_7JU6u2gRUNczK8PFXyfnsXRPNhv5`
- Production URL: `https://mara-vera-osqeu486t-ignacioalfaz-6766.vercel.app`
- Alias reported by Vercel: `mara-vera-ignacioalfaz-6766.vercel.app`
- Target: `production`
- Project name: `mara-vera`

Vercel initially reported deployment state `INITIALIZING`.

Automated follow-up inspection was blocked by an OAuth/team-scope mismatch (`403 Not authorized` for scope `ignacioalfaz-6766`). Therefore this document does **not** claim that the remote build reached READY. No build failure was reported by the deployment creation call; readiness remains externally unverified from the connector.

## Public deployment package

The Vercel deployment intentionally used a minimal standalone launch package rather than internal P0 surfaces. It includes:

- Home;
- adult age gate;
- AI disclosure;
- public text-first Mara experience;
- local browser continuity;
- legal/transparency page;
- responsive styling.

It excludes:

- P0 debug panels;
- test labs;
- payment simulation;
- real checkout;
- production voice claims;
- adult explicit production content.

## GitHub launch branch

Authoritative web branch remains:

`web/mvp-owned-funnel`

Public-facing branch changes immediately before launch include:

- `web/components/launch-experience.tsx`
- `web/app/experience/page.tsx` → public LaunchExperience
- `web/app/page.tsx` → prototype/P0 language removed from public Home

## Next operational priorities

Do not reopen broad architecture before these:

1. verify production deployment externally;
2. fix any build/runtime issue if present;
3. replace placeholder visual identity with canonical Mara assets;
4. integrate canonical Mara voice when quality is acceptable;
5. connect a stable production domain when chosen;
6. observe real public behavior before adding expensive infrastructure;
7. only later activate payments after provider/compliance authorization.

## Permanent launch principle

> **SHIP THE RELATIONSHIP THESIS BEFORE SHIPPING THE INFRASTRUCTURE DREAM.**
