Identity + Preference Memory is implemented as a stacked branch on top of `web/mvp-owned-funnel` (PR #4), because the live Public Alpha code is not yet merged into `main`.

PR #5's implementation PR should therefore target `web/mvp-owned-funnel` until PR #4 is merged; it can then be retargeted/rebased onto `main` without mixing the two outcomes.

No merge without `mergea`.
