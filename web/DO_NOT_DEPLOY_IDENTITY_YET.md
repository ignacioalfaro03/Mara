# DO NOT DEPLOY IDENTITY YET

The current canonical Public Alpha on PR #4 remains the production baseline.

This stacked branch adds account/database capability but must not replace the live Alpha until:

1. a dedicated Mara backend exists;
2. the migration is applied and RLS is verified;
3. the backend env is configured;
4. distinct same-Mara pose assets exist;
5. CI + browser smoke passes;
6. founder explicitly authorizes deployment.

No merge without `mergea`.
